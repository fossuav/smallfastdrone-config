# PROGRESS

Single source of truth for what's been done, what's in flight, and what's blocked.

Update alongside any meaningful change. Keep entries short. This file replaces memory storage for project state — don't store progress in `~/.claude/projects/.../memory/`.

## Status: Planning

Project is at the planning stage. No application code exists yet. The repo currently contains:

- Plan and architecture docs (this directory)
- LICENSE, README.md (stubs)

## Phases

| Phase | Status | Notes |
|---|---|---|
| Planning | ✅ Complete (2026-05-15) | Initial plan, architecture, bringup, security docs in place |
| Phase 0 — Scaffolding | ✅ Complete (2026-05-18) | All 13 planned slices landed (scaffold, lint, UI lib, router+views, Pinia+expert, SFD submodule + SITL, transport + bridge, MAVLink heartbeat, AUTOPILOT_VERSION, Playwright E2E, Tres.js 3D drone + logo, WebSerial, PWA). HTTPS dev (mkcert) plugin is installed but disabled — re-enable when WebAuthn / remote-key-exchange / LAN device testing actually needs HTTPS. |
| Phase 1 — MAVLink + params | ✅ Complete (2026-05-19) | node-mavlink (Phase 0), heartbeat + AUTOPILOT_VERSION (Phase 0). Full param browser: fetch, metadata-driven descriptions / units / range hints / enum dropdowns / decoded bitmasks, inline edit + dirty tracking + per-row undo + Discard, Apply (PARAM_SET ack + PREFLIGHT_STORAGE) with per-row state icons + summary banner. 5 E2E tests covering connect, browse, edit, enum dropdown, and Apply. |
| Phase 2 — Bringup wizard | ✅ Complete (2026-05-20) | Pluggable wizard runtime + library + runner + `frame-select` + `pid-autotune-pro` locked stub (slice A); `preflight` sub-wizard + `bringup` meta-wizard chaining the two with returnTo plumbing + per-FC completion tracker backed by localStorage + library Done badges + dynamic outcomes (slice B). 10 Playwright tests green. IndexedDB upgrade + full sub-wizard set (sensors, RC, motors, failsafes, …) land in later slices alongside the wizards that need them. |
| Phase 3 — Recipe library | ⏳ Not started | SFD-flavoured tuning recipes, dry-run + commit |
| Phase 4 — Log handling | ⏳ Not started | LOG_REQUEST_LIST/DATA pull, handoff to analysis-private |
| Phase 5 — DFU + Security seam | ⏳ Not started | WebUSB DFU flashing of SFD firmware via SignedArtifactUploader; no direct upload paths |
| Phase 6 — MSP + BLHeli passthrough | ⏳ Not started | Minimal MSP for BLHeli entry; full 4-way for ESC settings + firmware flash |

Test infrastructure (cross-cutting, lands during Phase 0 alongside the app shell):

| Item | Status | Notes |
|---|---|---|
| SFD submodule at `vendor/smallfastdrone/` | ✅ Landed (slice 7) | Pinned to SFD `SmallFastDrone-4.7-beta` HEAD `d0615774f4`. `dev-setup.sh` runs `git submodule update --init --recursive` (5-10 min first time) |
| SITL build orchestration | ✅ Landed (slice 7) | `bun run sitl:build` / `start` / `stop`. `dev-setup.sh` checks for gcc/g++/python3/ccache and warns if missing. |
| SITL bridge (`test/sitl/bridge.ts`) | ✅ Landed (slice 9-10) | Zero-dep Bun script; verified end-to-end forwarding SITL bytes to a WS client |
| Transport abstraction (incl. `WebSocketTransport`) | ✅ Landed (slice 9-10) | `src/transport/{types,websocket,webserial,select}.ts`; URL-param picker (`?transport=websocket&host=...`); WebSerial is stubbed until there's bench hardware |
| Playwright E2E (first test) | ✅ Landed | `playwright.config.ts` with two webServers (SITL+bridge, Vite); `test/e2e/connect.spec.ts` drives the connect → heartbeat → AUTOPILOT_VERSION flow against real SITL. Runs in ~17 s. |
| CI workflow | ⏳ Not started | Submodule checkout, ccache, layered test runs |

## Recent log

- 2026-05-22: **Motor-check slice 2a — correction maths + Vitest.** Stood up Vitest (PLAN decision 26, first unit tests in the repo): `vitest.config.ts` (node env, `test/unit/**`), `test:unit` / `test:unit:watch` scripts, README. New pure module `src/workflow/motor-check.ts` — `computeCorrections(geometry, observations, channelFunctions)` turns the operator's per-motor observations into the fixes: a `SERVOn_FUNCTION` remap for wrong motor order, and the set of output channels to toggle in `SERVO_BLH_RVMASK` for wrong direction, plus `orderOk` / `directionOk` / `inconsistent`. Isolated as side-effect-free logic and unit-tested (6 cases: all-clear; swapped pair → remap only, no false direction fault; reversed motor → reverse only; swap+reverse combined; inconsistent duplicate observation) because a wrong remap flips a drone. Relies on the SITL-verified chain `test order → mixer index → Motor function (33+idx) → output channel`. **Not yet wired into the wizard** — slice 2b applies it (param writes + reboot/reconnect/re-verify, gated on `SERVO_BLH_RVMASK` presence for direction since that's hardware-only) and turns the review screen from "report" into "fix". 6/6 unit tests green; lint + typecheck clean.
- 2026-05-22: **Motor-check UX revision (operator feedback).** Reworked the wizard after a first look: (1) **Real 3D model** — vendored Betaflight's `quad_x.gltf` (GPLv3, attributed in `src/assets/models/CREDITS.md`) in place of the procedural drone; it's a single mesh so per-motor highlight is overlaid as glowing rings (active = amber pulse, done = green, mismatch = red) at the motor positions. Loaded imperatively via three's `GLTFLoader` (recenter + scale + orient), rendered with `<primitive>`. (2) **Standard orientation** — model's nose points *away* from the operator (built-in forward arrow points to the far side); X-frame model rotated 45° for a Plus frame so its arms meet the rings. (3) **Clearer flow** — steps by **motor number** (Motor 1…N) showing where each *should* be ("Should be the Right motor") with that motor highlighted; **auto-spins** on entering each step (no separate "spin" click); the expected position is **pre-selected** so the common case is just confirm-direction → Next; and a **Back** button revisits earlier motors. Selection is labelled position buttons (replacing the spatial hotspots — clearer and decoupled from the camera angle). Build: added `three` + `three/examples/jsm/loaders/GLTFLoader.js` to `optimizeDeps.include` (a direct GLTFLoader import otherwise triggers a mid-test "new dependencies optimized" reload) and a `*.gltf?url` type decl. Verified the render via a throwaway Playwright screenshot. E2E updated to the new flow; all 14 green, lint + typecheck clean.
- 2026-05-22: **Motor-check wizard (bringup phase 04) — detect + report (slice 1 of 2).** New `motor-check` desktop wizard spins each motor in turn (real `MAV_CMD_DO_MOTOR_TEST` against SITL) and has the operator — **props off** — identify which motor physically moved and which way it turned, Betaflight-style but adapted to ArduPilot. Identification is deliberately *blind* (the motor under test isn't pre-highlighted) — that's what actually catches a mis-wire. Observations are compared against the firmware mixer geometry (transcribed from `AP_MotorsMatrix.cpp` MotorDef tables, cross-checked vs ArduPilot's documented quad-X diagram — treated as safety-critical) and reported per-motor. Pieces: `src/protocol/motors.ts` (`buildMotorTest`/`buildMotorTestStop`; PWM throttle type to skip the RC-cal gate — spins need landed + safety-off + no e-stop, all true on a propless bench); `src/workflow/motor-geometry.ts` (frame → ordered motors `{testOrder, motorIndex, angle, position, spin}`; quad X + Plus, structured to extend to hex/octo, returns null → "frame not supported yet" rather than guessing); `src/ui/visuals/MotorCheck3D.vue` (Tres top-down drone, frame-agnostic arm/motor/prop per angle, active-motor glow + prop spin in the expected direction); `src/wizards/motor-check/{manifest.ts,DesktopView.vue}` (safety-gate → spin → identify → review, mandatory props-off acknowledgement, always-available emergency **Stop**, motor stopped on unmount). Ships as a standalone bringup wizard in the library; chaining it into the full-bringup meta waits on hex/octo geometry so the meta runs on any frame (the meta test exercises a Hex). Click targets are **accessible HTML hotspots** layered over the 3D canvas (a WebGL mesh is neither keyboard- nor Playwright-clickable). New E2E `wizard-motor-check.spec.ts` drives the happy path end-to-end against SITL (quad +): props-off → 4 real spins → identify → all-clear → Done badge. All 14 Playwright tests green; lint + typecheck clean. **Decisions / next:** order errors will auto-fix by remapping `SERVOn_FUNCTION`; direction via `SERVO_BLH_RVMASK` (reboot to apply) — but `SERVO_BLH_*` is **not compiled into SITL** (`HAVE_AP_BLHELI_SUPPORT = HAL_SUPPORT_RCOUT_SERIAL`, undefined for the sim), so direction correction is a hardware-only capability the wizard will gate on the param's presence. **Slice 2** = auto-correction (remap permutation + RVMASK + reboot/reconnect/re-verify) with Vitest set up to unit-test the permutation math, plus hex/octo geometry and chaining motor-check into the bringup meta; the in-field Lua/CRSF version is a later slice. The full chain that drives the remap is now confirmed against SITL: `test_order → motor index (mixer) → Motor function (index+1) → output channel (SERVOn_FUNCTION)`.
- 2026-05-22: **Lua-engine wizards are now testable end-to-end against SITL — `imu-noise` live path green.** Bottomed out the long-standing "Lua not testable in SITL" blocker; `docs/TESTING.md` "Lua wizards in SITL" was wrong on every count and is rewritten. Findings, all proven against SITL with pymavlink before any UI was wired: (1) scripting **does** initialise after `SCR_ENABLE=1` — the `AP_PARAM_FLAG_ENABLE` "lands too late" theory was a misdiagnosis (works via boot defaults *and* via PARAM_SET+reboot); (2) the real blocker was a **path mismatch unique to SITL** — `SCRIPTING_DIRECTORY` is `./scripts` on posix but MAVLink-FTP writes to `APM/scripts/` land at `<workdir>/APM/scripts`, a different directory (on hardware both resolve to `/APM/scripts`), so the engine never saw the uploaded file; (3) scripts load **once** at scripting-engine start (`lua_scripts.cpp` `load_all_scripts_in_dir`) — a runtime FTP upload isn't auto-detected — but **`MAV_CMD_SCRIPTING` STOP_AND_RESTART** (param1=3) tears down + recreates the Lua state and rescans the dir, loading a freshly-uploaded applet **without a full FC reboot**. New wizard install path: **upload (FTP) → restart scripting → wait for the control param to appear → arm**; the only reboot in the Lua flow is the one-time `SCR_ENABLE` enable, which the Drone-settings page owns. Changes: `scripts/sitl-start.sh` symlinks `./scripts -> APM/scripts` (replacing the broken, now-deleted pre-place mechanism); `buildScriptingRestart()` (mavlink.ts) + `buildParamRequestRead()` (params.ts); `lua-engine.ts` gains `restartScripting()` / `readParam()` / `waitForControlParam()` and a corrected applet-load-model header note; `imu-noise` DesktopView runs upload→restart→load→arm and points the scripting-off operator at **Drone settings** (was "expert mode → Parameters", a microcopy violation — no param names in operator copy). New `test/e2e/wizard-imu-noise-live.spec.ts` drives the genuine flow end-to-end (enable scripting incl. reboot → upload → restart → arm → 0–100% progress → "Quiet/Normal/…" verdict → Done badge); existing `imu-noise-wizard.spec.ts` scripting-off assertions updated to the new copy. All 13 Playwright tests green; lint + typecheck clean. Closes the Phase 3 dependency for the first Lua-engine wizard.
- 2026-05-22: **Drone-settings page + reboot/auto-reconnect pattern.** New `/settings` route (`src/views/SettingsView.vue`) establishing the operator-facing pattern for FC feature toggles that map to params — scripting (`SCR_ENABLE`) is the first toggle. Two flows driven by a `rebootRequired` flag: **reboot-required** (scripting) stages a pending change on switch-flip and a single **Apply** click does the whole sequence — PARAM_SET + echo → storage settle → reboot → **automatic reconnect** (retries connect+heartbeat through the FC's restart window, ~60s budget) → params reload → applied, with a reconnect-failed fallback offering a manual Reconnect; **no-reboot** toggles (future) write the param immediately on flip with no Apply step. Phases: checking → unavailable / idle → pending → applying → restarting (reboot + auto-reconnect) → idle, plus reconnect-failed. Operator-first copy throughout; no `SCR_ENABLE`/`PARAM_SET` surfaced. Hardcoded for one toggle deliberately — lifts to a FeatureToggle registry when a 2nd lands. Supporting pieces: `buildPreflightReboot()` in mavlink.ts (COMMAND_LONG 246), `reboot()` action + `rebooting` intent flag in the session store (heartbeat handler clears it post-reboot), `/settings` route, and a **restored SITL restart-on-exit wrapper** in `sitl-start.sh` (subshell loops arducopter, trap pkills child on TERM) so `PREFLIGHT_REBOOT_SHUTDOWN` brings SITL back rather than ending the session. Two bring-up subtleties, documented inline: (1) wait ~1.5s after PARAM_SET before rebooting — ArduPilot auto-saves on PARAM_SET but SITL's storage backend batches the write and a reboot races the flush (note: `PREFLIGHT_STORAGE` param1=1 is DENIED by current firmware — only param1=2 erase works — so PARAM_SET's own save is the only path); (2) the reconnect loop must wait for the post-reboot heartbeat before `params.load()`, because `session.connect()` resolves on transport-open (before the heartbeat) and `params.load()` bails when `sysid` is null — this was the bug that made the value look reverted. New E2E (`settings-scripting.spec.ts`) drives the one-click flow and waits out the auto-reconnect. All 12 Playwright tests green; lint + typecheck clean. This also unblocks operator-driven scripting enable for the Lua wizards (no longer need it hardcoded in defaults).
- 2026-05-21: **Phase 2 slice C — Lua-engine architecture (FTP + LuaEngine + first Lua wizard).** Three commits in: (C1) `MavFtp` client at `src/protocol/ftp.ts` speaks FILE_TRANSFER_PROTOCOL — upload/download/removeFile/createDirectory — plus a Bun smoke test (`bun run ftp:smoke`) that does 742-byte round-trip against SITL. Documented two ArduPilot-specific quirks inline (OpenFileRO ACK size, ReadFile uses the request's `size` byte as the desired read length). (C2) `useLuaEngine()` composable at `src/workflow/lua-engine.ts` — checkScripting (loads params + reads SCR_ENABLE), uploadApplet (ensures APM dirs + FTPs the source), removeApplet (FileNotFound treated as success), setParam (PARAM_SET with echo, bypasses params store since Lua-declared control params aren't in the cached map), subscribeNamedValue (filtered NAMED_VALUE_FLOAT). (C3) IMU noise check wizard at `src/wizards/imu-noise/` — manifest declares `engines: [{ kind: 'lua', applet: 'applet.lua', requires: { scripting: true } }]`; applet.lua samples gyro magnitude at 20 Hz for 5s while WIZ_NOISE_ACTIVE=1, throttled progress + final result via NAMED_VALUE_FLOAT, self-disables on completion; DesktopView walks scripting check → upload → arm → progress bar → verdict ("Quiet" / "Normal" / "Noticeable" / "High") → cleanup, with operator-actionable fallback for SCR_ENABLE=0. EngineDescriptor extended with the `lua` variant. `*.lua?raw` import declared in vite-env.d.ts so wizards can bundle their applets as strings. SITL Lua testing isn't reachable end-to-end yet — SCR_ENABLE carries AP_PARAM_FLAG_ENABLE so scripting init runs before defaults apply; documented in docs/TESTING.md "Lua wizards in SITL" along with the three options for fixing it later. The shipped E2E exercises the scripting-off fallback path; the live path is validated on real hardware. All 11 Playwright tests green; lint + typecheck clean.
- 2026-05-20: **Phase 2 slice B — bringup meta-wizard + preflight sub-wizard.** Closes Phase 2's done-when ("operator can start a wizard, advance through Pre-flight and Frame phases, see gated progression, resume after disconnect"). New `preflight` desktop wizard owns no params — surfaces vehicle / autopilot / firmware / FC uid plus the SystemStatus subsystem row and asks the operator to confirm before bringup starts changing settings. New `bringup` meta-wizard at `src/wizards/bringup/` walks a fixed ordered list (`preflight` → `frame-select` today; grows to cover the full BRINGUP.md phase set in later slices) — each step links to the standalone sub-wizard URL with `?returnTo=/wizard/bringup` so the sub-wizard's back path flows back into the meta. Completion state per sub-wizard is read from the wizardProgress store; bringup auto-marks itself complete when every sub-wizard is complete (no separate Finish button — meta-wizards don't take actions of their own). Celebratory done banner appears once all steps are done. `frame-select` gained returnTo support so the same wizard works both standalone (back → library) and as a step inside bringup (back → bringup runner). Resume across disconnect is implicit: the library card carries the Done badge + outcome, and bringup re-reads the wizardProgress store on every mount, so an operator who unplugs mid-bringup comes back to exactly where they left off. New E2E spec drives bringup through both sub-wizards end-to-end; library connect-vehicle assertion loosened to `/Connected to your \w+/` because the earlier frame-select test persists FRAME_CLASS=2 so SITL reports as Hexacopter for later specs. All 10 Playwright tests green in ~37 s; lint + typecheck clean. Phase 2 now flagged Complete in the phase table.
- 2026-05-19: **Phase 2 slice A — wizard runtime + library shell + first real wizard.** New `src/workflow/wizard-runtime.ts` implements the manifest registry (Vite-glob scan of `src/wizards/*/manifest.ts`, lazy-loaded DesktopView per wizard), capability snapshot, and prereq checker. New `src/views/WizardLibraryView.vue` renders a card grid with unlocked wizards above a "Pro — coming soon" section, with friendly inline reasons for any unmet prereqs. New `src/views/WizardRunnerView.vue` is the per-wizard chrome — looks the manifest up by URL id, evaluates prereqs against the live capability snapshot, lazy-mounts the DesktopView, and falls back gracefully for unknown ids / locked URLs / unmet prereqs. Two real wizards land: `frame-select` (desktop engine — operator picks from 7 topdown SVG layouts for the common multicopter shapes, the wizard writes `FRAME_CLASS` + `FRAME_TYPE` via the params store's dirty/apply pipeline) and `pid-autotune-pro` (locked stub with `locked: true` + unlock_blurb so the commercial gating affordance is visible from day one). `/wizard` now serves the library; per-wizard runners live at `/wizard/:id`. Old `WizardView.vue` placeholder removed — its replacement, the bringup meta-wizard, becomes just another wizard in the library when Phase 2 slice B lands. Three new E2E specs (library + frame-select happy path with SITL cross-check, unknown id fallback, locked URL gating). All 9 Playwright tests green in ~35 s; lint + typecheck clean.
- 2026-05-19: **Pluggable wizard runtime — architectural pivot.** New `docs/WIZARDS.md` formalises the wizard contract before any Phase 2 code lands. Headline calls: (1) wizards are the single delivery primitive — recipes get subsumed as degenerate wizards (`engines: [desktop]`, one step, no live state); (2) **engine vs view split** — three engines (`lua`/`log`/`desktop`), two views (`desktop` required, `crsf` optional/informational), runtime picks engine per FC capability so operator never sees the choice; (3) **Lua lifecycle is upload-on-start / remove-on-completion**, not dormant-while-idle — finished wizards reclaim their full FC resource footprint via FTP delete of the applet, with `WIZ_<ID>_ACTIVE` as the in-session control param + orphan detection at reconnect; (4) bringup becomes a meta-wizard that chains independently-runnable sub-wizards; (5) `locked: true` flag is the commercial gating seam — v1 just greys the card with a "Pro" badge, real entitlement check lands when there's a payment integration to wire to. Anti-patterns section enforces the "opinionated, not interrogating" rule — any wizard that asks a configuration question isn't ready to ship. PLAN.md reshaped: Phase 2 = wizard runtime + library + `frame-select` + `bringup` meta + one locked stub; Phase 3 = SFD desktop-engine wizards (cinewhoop, throw, failsafes) + first Lua-engine wizard (lifecycle plumbing); Phase 4 = log handling + narrow in-tool .bin parser + first log-engine wizard (`notch-from-hover`). BRINGUP.md now points at WIZARDS.md and reframes each phase as a sub-wizard.
- 2026-05-19: **Arm-readiness status panel + colour-coded bell.** New `src/ui/components/SystemStatus.vue`: a Betaflight-style horizontal row of 9 subsystem icons (Gyro, Accel, Compass, Baro, GPS, AHRS, Battery, RC, Pre-arm) on the Connect view, each driven by the corresponding SYS_STATUS bit (present + enabled + healthy → green; present + enabled + unhealthy → amber; otherwise muted). A "✓ Ready to arm" / "Not ready to arm yet" verdict beneath, derived from the prearm bit. Operator can see at a glance whether the drone is willing to arm without opening the bell. SITL doesn't stream SYS_STATUS to fresh clients by default, so the session store now sends `REQUEST_DATA_STREAM(MAV_DATA_STREAM_ALL, 2 Hz, start=1)` alongside `AUTOPILOT_VERSION` + `DO_SEND_BANNER` on first heartbeat — that also unblocks `ATTITUDE`/`VFR_HUD`/`GPS_RAW_INT` etc. for later slices. The message bell stays (operator still wants the full message log) but each row now has a severity-coloured left border matching the toast thresholds — red for EMERGENCY..ERROR, amber for WARNING, primary blue for NOTICE/INFO, muted for DEBUG. New E2E assertions for the status panel; connect spec's boot-line check switched to `.first()` because both regex branches match in practice. All 6 Playwright tests green in ~43 s.
- 2026-05-19: **Recent-messages bell in nav.** New `src/ui/components/MessageBell.vue`: a bell-icon UButton wrapping a UPopover that lists the session store's `recentStatusTexts` (reversed so newest is on top). Each entry shows a severity-coloured icon, the text (with break-words so long ones wrap), and a relative timestamp ("just now" / "12s ago" / "3m ago" / "1h ago"). Count badge on the bell trigger when entries exist. Empty state when disconnected. Placed in the nav header next to the Expert toggle. Connect E2E extended: opens the bell after connect, asserts the SFD banner ("ArduCopter V…SFD") and a routine boot line ("ArduPilot Ready" / "Barometer 1 calibration") are both visible in the popover. Also hardened `playwright.config.ts`: SITL+bridge entry no longer honours `reuseExistingServer` — a stale-bridge-with-dead-SITL state caused by leaving `bun run dev:sitl` running and then invoking `bun run test:e2e` would silently break every test. SITL startup is cheap enough (~2 s) that always-restart is the right default. All 6 Playwright tests green in ~43 s.
- 2026-05-19: **STATUSTEXT capture + SmallFastDrone detection.** Session store now subscribes to STATUSTEXT (msgid 253). On first heartbeat we send `DO_SEND_BANNER` (ardupilotmega cmd 42428) alongside the AUTOPILOT_VERSION request — ArduPilot replies with the human-readable banner including the `-SFD` suffix that the structured AUTOPILOT_VERSION fields don't carry. Any STATUSTEXT containing `SFD` flips `isSfd` true and the autopilot label becomes "SmallFastDrone" instead of "ArduPilot" — operator-friendly, matches what the FC actually identifies as. Ring buffer of last 50 STATUSTEXTs in `recentStatusTexts` for a future log pane. Messages with severity ≤ WARNING surface via `@nuxt/ui`'s `useToast` — error toast for severity 0-3, warning toast for severity 4. NOTICE/INFO/DEBUG are too chatty during boot (every EKF init line) to interrupt with. Connect E2E updated: now asserts "SmallFastDrone 4.x.x-beta (hash)" instead of "ArduPilot …", proving the banner detection works end-to-end. All 6 Playwright tests pass in ~26 s.
- 2026-05-19: **Bitmask + Values editor polish.** Two iterations after first ship of slice E: (1) Replaced the strict `<select>` for Values metadata with a free-text input + inline suggestion chips so float params with documented Values (filter freqs, PID example values) accept arbitrary input — Values are hints, not constraints; (2) HTML5 `<datalist>` filtered options by current input which hid valid suggestions, so swapped for always-visible right-aligned chip buttons; (3) Added per-bit checkbox row for params with Bitmask metadata (e.g. `FENCE_TYPE`'s 4 bits) — toggling updates the integer in the input without committing, operator commits via Enter/blur after flipping multiple bits. `@mousedown.prevent` on chip + checkbox wrappers keeps the input focused so blur-commit doesn't race the click. Two new E2E tests; full suite now 6 tests / ~25 s.
- 2026-05-19: **Phase 1 slice E — type-aware editors.** Uses the bundled SFD metadata to render: (a) a `<select>` dropdown in edit mode for params with `Values` metadata (e.g. `RTL_ALT_TYPE` → "0 — Relative to Home" / "1 — Terrain"); (b) a small italic caption beneath the value showing the decoded enum label (Values) or set bits joined with " | " (Bitmask, e.g. `RTL_OPTIONS = 5` → "UseRTLOnAbort | DualAircraftSynchronised"); (c) a `Range: low to high` tooltip on the input + display button when `Range` metadata is present. Bitmask *editor* (checkboxes for individual bits) deferred — display side is the operator-useful half. New E2E asserts `RTL_ALT_TYPE` opens as a dropdown with the expected option labels, selecting "Terrain" commits the edit, and the "Terrain" label appears in the value cell. 5 Playwright tests pass in ~23 s. Phase 1 now flagged Complete in the phase table.
- Earlier today (2026-05-19): **bumped dirty-row highlight** — Mission-Planner-style gold bg + 4 px left stripe so changed rows pop at a glance.
- 2026-05-19: **Phase 1 slice D — Apply: write pending edits to the drone.** Apply button is live. Per dirty param: sends `PARAM_SET`, waits ≤1.5 s for the FC's `PARAM_VALUE` echo, classifies as `acked` (echo matches the requested value within float epsilon — clears the edit, updates store), `mismatched` (echo arrived with a different value — FC clamped — updates store but leaves the edit so operator sees "asked X, got Y"), or `failed` (no echo). When at least one param landed, sends `MAV_CMD_PREFLIGHT_STORAGE(param1=1)` best-effort (ArduPilot auto-saves param changes within ~10 s and SITL doesn't ack the command, so no-response is informational not error). View gains a "Writing…" / "Saving…" progress banner during apply, a green/amber result banner after, and per-row state icons (spinner / check / warning / error). New E2E (Apply + RTL_ALT_FINAL_M ← 1234) green; full Playwright suite now 4 tests / ~31 s. Protocol helpers: `buildParamSet`, `buildPreflightStorageSave`, `MSGID_COMMAND_ACK`, `PREFLIGHT_STORAGE_PARAM_SAVE`.
- 2026-05-19: **Phase 1 slice C — param editing + dirty tracking (UI only).** Click any value cell in the param browser to edit inline (text input, Enter commits, Esc cancels, blur commits). Dirty rows get a gold accent on the new value plus a small "was X" line and an undo-icon revert button. Top-of-card banner shows "N changes pending" with Discard (clears all) and an Apply button that's disabled with a tooltip pointing at the next slice. New "Show changes only" toggle filters to just the dirty rows. Store gains `edits` map + `dirtyCount` / `dirtyList` / `isDirty` / `effectiveValue` / `setEdit` / `revertParam` / `discardAll`. Reload clears edits (refresh = "give me current FC state"); conflict surfacing deferred. New E2E test exercises the full flow: edit RTL_ALT row → banner → "was X" → Discard. All 3 Playwright tests green (27 s total).
- 2026-05-18: **Phase 1 slice B — param metadata from the SFD source.** `scripts/build-param-metadata.py` invokes ArduPilot's own `Tools/autotest/param_metadata/param_parse.py --vehicle ArduCopter --format json` against the vendored submodule, flattens the grouped output into a `name → {displayName, description, units, range, bitmask, values, user, rebootRequired}` map, drops empty fields, sorts, and writes minified to `src/protocol/param-metadata.json` (~1.6 MB, 5,704 params). `bun run params:rebuild` re-runs after a submodule bump. `src/protocol/params.ts` imports the JSON + exposes `getParamMeta(name)`. `ParamsView` swaps the Type column for a human-readable "Description" column (showing displayName, falling back to first sentence of description; full description in the row's `title=` for hover), and renders units inline with the value. Bundle precache grew 2.6 → 4 MB raw — paid only when the operator opens /params. E2E for the param browser still green. ESLint configured to ignore the generated JSON.
- 2026-05-18: **Phase 1 slice A — read-only param browser.** `src/protocol/params.ts` exposes `buildParamRequestList`, `formatParamValue`, `paramTypeLabel`, `MSGID_PARAM_VALUE`. `src/stores/params.ts` runs the `PARAM_REQUEST_LIST` → stream-of-`PARAM_VALUE` loop in-store (uses the new `sendMessage`/`subscribeMessages` helpers on the session store so the raw MavLinkSession stays private); progress + completion tracked by `paramIndex`/`paramCount`, 10 s silence timeout for stuck fetches. `src/views/ParamsView.vue` is no longer a placeholder: auto-loads on mount when connected, shows a progress bar during the fetch, presents a searchable Name/Value/Type table once loaded. New E2E `test/e2e/params.spec.ts` flips expert mode → navigates to `/params` → asserts the fetch completes (1,384 params from SITL copter in ~4 s) and the ATC_filter narrows the table. Editing + commit (PARAM_SET, PREFLIGHT_STORAGE write) come in the next slices.
- 2026-05-18: **Phase 0 complete — PWA + HTTPS-dep installed.** `vite-plugin-pwa` generates a service worker (auto-update), web manifest with FOSS purple theme, and the standard PWA + favicon icon set (192 / 512 / 512-maskable, generated from sfd-logo on the brand purple via PIL). Build now produces `dist/sw.js` + `dist/workbox-*.js` + manifest with 18 precache entries (~2.5 MB raw — heavy because node-mavlink + three.js are in there, but acceptable for an installed app). `vite-plugin-mkcert` is installed but intentionally not wired into the plugin list yet — its `mkcert -install` step needs sudo, which Playwright's non-TTY webServer can't prompt for, and localhost is already a secure context for our APIs (service worker, WebSerial). Will re-enable when WebAuthn / remote-key-exchange / LAN device testing genuinely needs HTTPS. E2E remains green. Phase 0 status flipped to Complete.
- 2026-05-18: **WebSerial transport implemented.** `src/transport/webserial.ts` is no longer a stub — it now does `navigator.serial.requestPort()` (user-gesture-triggered from the Connect button), opens at 115200 8N1, runs a reader loop emitting `'data'` events into the existing session pipeline, handles user-cancel of the port picker with a friendly "No drone selected" message, and re-uses the same Transport interface as WebSocket so the session store / MAVLink session / UI didn't need changes. `describePort()` populates the transport description with VID/PID once the port is open, so the UI's "Connected via …" line shows which serial device the operator picked. Added `@types/w3c-web-serial` (Web Serial isn't in standard lib.dom yet) and registered it in tsconfig.app.json. ConnectView footer message updated from "Real-drone (USB) support arrives in a later slice" to instructions for both USB and SITL flows. **Written blind — no bench device handy.** SITL E2E still green, so the production code path is uncontaminated; real-hardware verification is on the operator's side when convenient.
- 2026-05-18: **Phase 0 slice 6 — Tres.js 3D drone + logo in nav.** First hero visual: `src/ui/visuals/Drone3D.vue` is a slowly-rotating X-quad built from Tres.js primitives in the FOSS UAV palette (purple frame, gold motors + translucent props), driven by `@vueuse/core`'s `useRafFn`. Replaces the static logo on the Connect-screen splash. Tres.js 5.x uses `useLoop` (not `useRenderLoop`) and only inside the canvas context, so we drive rotation from RAF instead — works regardless. App.vue swaps the "SFD Config" text in the nav header for the SFD logo at h-8 with `dark:invert`. `.gitignore` gains Playwright's `test-results/` + `playwright-report/`. Verified: existing E2E (connect → heartbeat → AUTOPILOT_VERSION) still green. ConnectView lazy chunk grew 254 KB → 474 KB gzip (Three.js + Tres + Cientos); acceptable per dep-budget decision but worth a manualChunks split later if it becomes painful.
- 2026-05-18: **First Playwright E2E green.** `test/e2e/connect.spec.ts` drives Chromium through `/?transport=websocket&host=...` → Connect → asserts "Connected to your Quadcopter" + a regex-matched firmware string + Disconnect. `playwright.config.ts` has two `webServer` entries — `scripts/test-sitl-bridge.sh` (foreground bridge with SITL started in the background, cleaned up on EXIT) and `bun dev` — so the test environment is self-sufficient (`bun run test:e2e` is enough). Fixed two issues found while standing this up: (1) the AUTOPILOT_VERSION render was missing a space between "ArduPilot" and the version string because the Vue `<template>` between interpolations dropped the whitespace after lint reformat — replaced with a `computed` that joins in JS; (2) `vite.config.ts` now has `optimizeDeps.include` listing node-mavlink, mavlink-mappings, and the polyfill shims so cold-cache runs don't trigger a "new dependencies optimized" reload mid-test. Test takes ~17 s end-to-end (Vite startup + connect + heartbeat + version + disconnect). `dev-setup.sh` installs the Chromium browser and probes for missing system libs.
- 2026-05-18: **AUTOPILOT_VERSION request/response.** First use of `MavLinkSession.serialize()` — on first heartbeat, the session store sends a `COMMAND_LONG(REQUEST_MESSAGE, 148)` to the FC. AUTOPILOT_VERSION (msgid 148, defined in the `standard` dialect — added that registry to the merged set) lands shortly after and is decoded into `4.7.0-beta (d0615774)`-style strings (`flight_sw_version` packed major/minor/patch/fw_type + `flight_custom_version` ASCII git hash). Connect view's autopilot row now reads "ArduPilot 4.7.0-beta (d0615774)". Verified end-to-end against SITL: decoded `type=128 hash='d0615774'` matching our pinned SFD commit. Uses node-mavlink's typed `CommandLong` / `AutopilotVersion` classes throughout — first proof the new MAVLink stack is good for request/response, not just passive heartbeat decode.
- 2026-05-18: **MAVLink parser switched to node-mavlink + polyfills** (refinement of slice 11, after user-led discussion). Discovered `mavlink-mappings`'s `MavLinkData` base class is empty — serialize/deserialize live in `node-mavlink`. To keep ecosystem alignment + get typed classes for every message + CRC validation + serialize-for-sending, swapped in node-mavlink with `vite-plugin-node-polyfills` (buffer/stream/events/process/util). `src/protocol/mavlink.ts` is now a thin wrapper exposing `MavLinkSession` (feed/serialize/on) with a registry merging minimal+common+ardupilotmega. Verified end-to-end against SITL: 3 heartbeats parsed correctly. ConnectView lazy chunk grew 15.9 KB → 254 KB gzip; acceptable per dep-budget decision. PLAN row 4 revised.
- 2026-05-18: **Phase 0 slice 11 — MAVLink heartbeat parsing.** `src/protocol/mavlink.ts`: an in-house Uint8Array streaming v2 frame splitter + HEARTBEAT decoder + operator-friendly labels (vehicle type, autopilot family, system state). PLAN row 4 revised: node-mavlink / mavlink-mappings per-message decoders all rely on Node Buffer/stream and don't bundle for the browser; we hand-roll framing against DataView and pull just the typed enums from `mavlink-mappings`. Session store now wires the parser into the transport's data stream, exposes reactive `sysid`/`vehicleType`/`autopilot`/`systemStatus`/`hasHeartbeat`. ConnectView replaces the bare byte counter with "Connected to your Quadcopter" + a dl of autopilot / sys ID / state / link bytes. Verified end-to-end against SITL via the bridge: parser correctly decoded heartbeats as `sys=1 type=2 (Quadcopter) autopilot=3 (ArduPilot) state=1 mavlink=v3`. Lazy chunk for ConnectView jumped 3.5 KB → 15.9 KB to absorb parser + enums.
- 2026-05-18: **Phase 0 slices 9–10 — transport abstraction + SITL bridge.** First end-to-end browser↔drone connection. Files: `src/transport/{types,websocket,webserial,select}.ts` (interface + WebSocket impl + WebSerial stub + URL-param picker), `src/stores/session.ts` (connect/disconnect/byte counter), updated `ConnectView` (live button + status), `test/sitl/bridge.ts` (zero-dep Bun WebSocket↔TCP bridge), `bun run bridge:start` script, `@types/bun` dev dep, eslint config relaxes `no-console`/`process` for `test/` + `scripts/` Bun CLI files. Verified end-to-end with a Bun WS client: 317 bytes / 5 s from SITL through bridge to the client. URL param `?transport=websocket&host=localhost:5761` selects the WebSocket transport; default falls back to a WebSerial stub that tells the operator how to use SITL until WebSerial is wired up. Production code does not import test transports.
- 2026-05-18: **Phase 0 slice 7 — SFD submodule + SITL build orchestration.** SmallFastDrone added as a git submodule at `vendor/smallfastdrone/`, pinned to current `SmallFastDrone-4.7-beta` HEAD (`d0615774f4`). Scripts added: `scripts/sitl-build.sh` (waf configure --board sitl + ./waf copter), `scripts/sitl-start.sh` (runs ArduCopter SITL on TCP 127.0.0.1:5760 from a temp workdir, pidfile-tracked), `scripts/sitl-stop.sh`. `bun run sitl:build` / `sitl:start` / `sitl:stop` wired in `package.json`. `dev-setup.sh` extended to recursively init submodules and probe for SITL build prerequisites. Verified: setup completes; recursive submodule init pulled mavlink, ChibiOS, DroneCAN, gtest et al. SITL build run-time on this machine still being measured.
- 2026-05-18: **Phase 0 slice 5 — Pinia + @vueuse/core + expert-mode toggle.** First Pinia setup store (`src/stores/ui.ts`) holds the expert flag, persisted per-session via `useSessionStorage` (clears on tab close, per UX.md). A `<USwitch color="secondary">` in the nav-bar header toggles it. Added `/params` route with `meta.expert: true`; nav items filter on `expert`, so the Parameters tab appears only when the switch is on. The route URL stays reachable for deep-linking either way — the toggle gates *visibility in nav*, not access. Lazy bundle: ParamsView 0.97 KB. Main bundle 421 KB / 131 KB gzip (+16 KB raw, +5 KB gzip for Pinia + the parts of @vueuse we use).
- 2026-05-18: **FOSS UAV brand palette applied.** Sampled from `fossuav_logo.PNG`: purple anchor `#4A1E80`, gold anchor `#C9A35F`. Hand-derived 11-stop scales (`foss-*`, `gold-*`) defined in `src/assets/css/main.css` via Tailwind 4 `@theme`, wired into Nuxt UI as `primary: foss`, `secondary: gold`, `neutral: neutral`. Updated `docs/UX.md` "Visual language" with a Brand palette subsection documenting the choice.
- 2026-05-18: **Phase 0 slice 4 — app shell with 6 routes**. `src/router.ts` defines lazy-loaded routes for Connect / Bringup / Recipes / Logs / Firmware / ESC tools, each with operator-friendly placeholder content. `App.vue` is now the shell: `<UApp>` + top nav bar (`<UNavigationMenu>` driven by route metadata) + `<RouterView>`. Splash card moved into `ConnectView`. Build now chunks each view (~0.8 KB lazy per route). Main bundle: 405 KB / 127 KB gzip.
- 2026-05-18: **Phase 0 slice 2 — Nuxt UI 4 + Tailwind 4** wired in. Vite plugin (`@nuxt/ui/vite`) + Tailwind/`@nuxt/ui` CSS imports + vue-plugin in `main.ts`. Landing page now renders a styled `UCard` with a disabled "Connect drone" button. Discovered Nuxt UI 4 requires vue-router (its `<NuxtLink>` override imports `useRoute`), so vue-router 5 came along with this slice — a minimal empty-routes setup until the app-shell slice fills it in. PLAN/CLAUDE updated from "vue-router 4" → "vue-router 5" (current stable). Build: 678 modules, 270 KB JS / 175 KB CSS (gzip 92 / 23). Lint, typecheck, dev server all green.
- 2026-05-18: **Feedback-into-playbooks discipline pinned.** Substantive user guidance lands in the relevant playbook (`CLAUDE.md`, `PLAN.md`, `PROGRESS.md`, `docs/*`) in the same change — not just applied in-the-moment. Playbooks always reflect current intent. Revisions to prior decisions go in the original row, not bolted on top. Captured in CLAUDE.md "How we work".
- 2026-05-18: **Working principles pinned.** Bias for working code — every step ships something runnable and reviewable, no big-bang phases. Plan is provisional — phases / decision rows can shift as we learn what the operator actually wants; updating PLAN.md + PROGRESS.md is normal, not exceptional. Captured in CLAUDE.md "How we work" and PLAN.md "Phased delivery" preamble.
- 2026-05-18: **Test infrastructure plan.** Added [docs/TESTING.md](docs/TESTING.md). SFD becomes a git submodule at `vendor/smallfastdrone/` (URL: `https://github.com/fossuav/smallfastdrone.git`) so we can build SITL and validate the tool against real ArduPilot in CI. Test pyramid: Vitest unit (fixtures), Vitest integration (Bun→TCP→SITL), Playwright E2E (browser→WebSocket bridge→SITL). Zero-dep Bun bridge at `test/sitl/bridge.ts`. Test transport selected via URL param — production bundle contains no test code. Each phase now has a paired test acceptance criterion. Phase 0 expanded to include the full test infrastructure. Added `playwright` to dev deps. See PLAN.md decisions rows 26–30 and updated Phase 0.
- 2026-05-18: **Coding standards adopted.** Added [docs/CODING-STANDARDS.md](docs/CODING-STANDARDS.md). Adopted **Vue.js official Style Guide** (Priority A + B) as authoritative Vue conventions doc; **`@antfu/eslint-config`** for enforcement (Vue + TS + formatter in one); `.editorconfig` for editor consistency. Includes an explicit "AI slop" anti-pattern list. **Walked back the Biome decision** (PLAN.md row 12 revised) — Biome's Vue SFC support is not first-class in 2026, and antfu's config is the ecosystem standard. See PLAN.md decisions rows 12, 23–25.
- 2026-05-15: **Operator-first repositioning.** Tool is for operators, not experts. Added [docs/UX.md](docs/UX.md) — operator-first design playbook (visual language, microcopy rules, expert-mode definition, anti-patterns). Param browser becomes expert-mode-only; wizard + recipes are primary surfaces. Added `@tresjs/core` + `@tresjs/cientos` (3D drone hero) and `@vueuse/core` (composition utilities) to dep budget. Updated bringup phases to specify a hero visual per phase. Updated recipe presentation: illustrated cards with outcome names, not parameter lists. See PLAN.md decisions rows 18–22.
- 2026-05-15: **Scope expansion** — added DFU firmware flashing (Phase 5, paired with security seam) and MSP + BLHeli 4-way passthrough (new Phase 6). DFU uses WebUSB; MSP+4-way ride WebSerial alongside MAVLink. ArduPilot's `AP_BLHeli` confirmed as the FC-side MSP+4-way bridge. Mission Planner explicitly displaced for these two flows. See PLAN.md decisions rows 6, 6a–6c and Phases 5–6.
- 2026-05-15: Modernized stack — TypeScript end-to-end, **Bun** as package manager + runtime, **Biome** replacing ESLint + Prettier, **dropped i18next and Storybook** from v1 scope, **dropped Babel/polyfills/CommonJS** (pure ESM, evergreen Chromium target). Added v1 dependency budget. See PLAN.md decisions rows 2, 11–17.
- 2026-05-15: Initial plan, architecture, bringup, and security docs created. Tech stack and scope decisions locked. Same family as betaflight-configurator with MAVLink replacing MSP.

## Open questions

- Param metadata schema — consume `apm.pdef.xml` from a smallfastdrone build, or derive at runtime from MAVLink? Lean: build-time XML import.
- node-mavlink browser bundle size after dialect generation. Spike in Phase 0.
- WebSerial UX for USB permission re-prompt across sessions — document for operators.
- WebUSB device-claim handoff when the FC switches between CDC-ACM (MAVLink) and DFU enumerations.
- Wizard state machine: hand-rolled vs XState. Decide at Phase 2 close.
- ESC firmware blob source — bundled, fetched on demand, or operator-supplied? Decide at Phase 6 entry.

## Blockers

None.
